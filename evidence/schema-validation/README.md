# Schema Validation: Rejection

This proves that Zod schema validation catches invalid artifacts before replay. The validation fails at parse time, identifying which field or constraint was violated. Review the validation error message to see the exact schema mismatch.
