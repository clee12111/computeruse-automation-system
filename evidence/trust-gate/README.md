# Trust Gate: Capability Blocked

This proves that the trust gate blocks unapproved capabilities from replay. The gate checks capability approval status before execution and rejects the request. Review the gate decision log to see which approval rule was violated and why the capability was blocked.
