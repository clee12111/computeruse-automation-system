# Pre-Flight Rejection: Invalid Input

This proves that the safety gate catches invalid inputs before launching the browser. The replay exits with validation error before any interaction occurs. Review the pre-flight checks and schema validation logs to see which input constraint was violated.
