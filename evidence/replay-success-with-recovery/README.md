# Success with Error Recovery

This proves that the replay engine detects runtime errors (SESSION_EXPIRED), applies recovery logic from the artifact, and continues to completion with SUCCESS. Start with the error detection point in the transcript, then trace the recovery branch through to successful completion.
