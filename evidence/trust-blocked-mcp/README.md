# Trust Blocked: MCP Response

This proves that the MCP server can return trust_blocked responses to reject unsafe operations. The transcript shows the MCP rejecting a request before any side effect occurs. Check the MCP protocol exchange to see the rejection message and reasoning.
