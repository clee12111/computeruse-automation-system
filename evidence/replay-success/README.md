# Replay Success with Output Extraction

This proves that the replay engine executes an artifact deterministically, extracts structured output (savingsBalance), and completes with SUCCESS result. Start with the replay transcript to see step-by-step execution, then verify the output extraction against the artifact schema.
