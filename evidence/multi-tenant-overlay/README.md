# Multi-Tenant Overlay: Same Artifact, Different Tenants

This proves that one artifact can execute successfully across two different tenants using overlay configuration. Both replays reach SUCCESS with identical capability but tenant-specific overrides (credentials, selectors, wait times). Compare the two replay transcripts to see how overlay context switches between tenants.
